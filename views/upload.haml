= flash[:notice]

%h1 Select a Zipfile (or not) to upload

%form{ action: "/upload", method: "POST", enctype: "multipart/form-data" }
  %input{ type: "file", name: "file" } 
  %input{ type: "submit", value: "Upload!" }

